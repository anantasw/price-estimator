import React from "react";

const AddressAutocomplete = ({ placeholder, className }) => {
  return <input type="text" placeholder={placeholder} className={className} />;
};

export default AddressAutocomplete;
